import 'package:flutter/material.dart';

BuildContext contextConstants;

final screenHeight = MediaQuery.of(contextConstants).size.height;
final screenWidth = MediaQuery.of(contextConstants).size.width;